集群

  主节点：用来读取或者更新数据，采用日志的方式把数据同步给从节点（异步方式）
  从节点：主要用来读取数据，没有写操作
  最终一致性：同一时刻数据可能不一致，过一段时间后数据保持一致性
  实时一致性：同一时刻数据一致
  心跳：客户端每隔几秒发送一次请求，服务端返回包，假如连续几次木有收到回复，则认为此节点挂掉，踢出集群

  handlerRemove：可处理链接断掉的问题，但是手机客户端和服务器端建立好长连接，客户端开启长连接，服务
  端不会触发接受remove方法，抛出异常。